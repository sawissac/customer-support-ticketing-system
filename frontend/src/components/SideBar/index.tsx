import { IconHash } from "@tabler/icons-react";
import React from "react";

const SideBar = () => {
  return (
    <div>
      <div>
        <IconHash />
      </div>
    </div>
  );
};

export default SideBar;
